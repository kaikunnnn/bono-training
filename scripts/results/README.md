# Subscription Sync Results

These JSON files contain the results of subscription sync scripts.
They are kept for audit purposes.

- fix-subscription-sync-*.json: Phase 1 results (migrated user sync)
- fix-expired-subscriptions-*.json: Phase 2 results (expired user processing)

