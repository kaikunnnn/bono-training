# 記事詳細ページ - TODO（身につけること）セクション デザイン仕様書

## 📋 概要

記事詳細ページで、ユーザーが記事を通じて学ぶ・身につけるべき項目を箇条書きで表示するセクションです。ヘッダーブロック「身につけること」の下に、複数の TODO 項目をリスト形式で表示します。

---

## 🎯 構造概要

```
todo（幅720px）
├── Heading-block（ヘッダー領域）
│   └── title: "身につけること"
└── wrap_content（コンテンツラッパー）
    ├── item_todo
    │   ├── circle（チェックマーク用の円）
    │   └── text（タスク内容）
    ├── item_todo
    │   ├── circle
    │   └── text
    └── ... （必要なだけ繰り返し）
```

---

## 📐 全体コンテナ

### 外側フレーム（todo コンポーネント）

| プロパティ           | 値                                         |
| -------------------- | ------------------------------------------ |
| **コンポーネント名** | todo                                       |
| **幅**               | 720px（固定）                              |
| **高さ**             | Hug（コンテンツに自動調整）                |
| **レイアウトモード** | Row（実装上は Column 推奨）                |
| **パディング**       | 2px（外側の枠線用）                        |
| **背景色**           | #FFFFFF（白）                              |
| **ボーダーラジアス** | 12px（全角）                               |
| **シャドウ**         | 1px 1px 4px 0px rgba(0,0,0,0.08)（淡い影） |

### 内側コンテナ（Container）

| プロパティ           | 値                                 |
| -------------------- | ---------------------------------- |
| **レイアウトモード** | Column（縦並び）                   |
| **パディング**       | 0px（外側の 2px で調整済み）       |
| **ギャップ**         | なし（ヘッダーとコンテンツは密着） |
| **サイズモード**     | Fill（横方向）/ Hug（縦方向）      |

---

## 🔤 Section 1: ヘッダーブロック（Heading-block）

### レイアウト情報

| プロパティ           | 値                                |
| -------------------- | --------------------------------- |
| **レイアウトモード** | Row（横並び）                     |
| **配置**             | Center                            |
| **ギャップ**         | 10px                              |
| **パディング**       | 5px 16px（上下 5px、左右 16px）   |
| **背景色**           | #F5F5F5（ライトグレー）           |
| **ボーダーラジアス** | 10px 10px 0px 0px（上部のみ丸角） |
| **サイズモード**     | Fill（横方向）/ Hug（縦方向）     |

### タイトルテキスト（"身につけること"）

| プロパティ           | 値                |
| -------------------- | ----------------- |
| **要素名**           | 身につけること    |
| **テキスト**         | "身につけること"  |
| **フォント**         | Noto Sans JP      |
| **フォントサイズ**   | 12px              |
| **フォントウェイト** | 700（Bold）       |
| **行高**             | 24px（2em）       |
| **文字間**           | 標準              |
| **色**               | #656668（グレー） |
| **配置**             | LEFT / TOP        |
| **サイズモード**     | Hug（自動）       |

### データ例

```json
{
  "sectionTitle": "身につけること"
}
```

---

## 📝 Section 2: コンテンツラッパー（wrap_content）

### レイアウト情報

| プロパティ           | 値                              |
| -------------------- | ------------------------------- |
| **レイアウトモード** | Column（縦並び）                |
| **パディング**       | 12px 8px（上下 12px、左右 8px） |
| **ギャップ**         | 12px（各 TODO 項目間）          |
| **背景色**           | #FFFFFF（白、継承）             |
| **サイズモード**     | Fill（横方向）/ Hug（縦方向）   |

### 構造

複数の `item_todo` コンポーネントを縦方向にスタック。各項目は 12px のギャップで区切られます。

---

## ✓ TODO 項目（item_todo）

### 全体レイアウト

| プロパティ           | 値                                 |
| -------------------- | ---------------------------------- |
| **コンポーネント名** | item_todo                          |
| **レイアウトモード** | Row（横並び）                      |
| **配置**             | Center（縦方向）                   |
| **ギャップ**         | 10px                               |
| **パディング**       | 0px 8px（左右 8px）                |
| **幅**               | 816px（外側の 2px を含むと 720px） |
| **高さ**             | Hug（自動）                        |

### 構成要素

```
item_todo（幅816px）
├── circle（6x6px チェック用の円）
└── text（タスク内容テキスト）
```

---

### 2-1. チェックマーク用円（circle）

#### レイアウト情報

| プロパティ           | 値                                         |
| -------------------- | ------------------------------------------ |
| **要素名**           | circle                                     |
| **要素タイプ**       | Frame（装飾用）                            |
| **幅**               | 6px（固定）                                |
| **高さ**             | 6px（固定）                                |
| **ボーダーラジアス** | 10000px（完全な円）                        |
| **背景色**           | rgba(23, 23, 23, 0.64)（濃グレー・半透明） |
| **レイアウト**       | Center（内部は使用しない）                 |

### 状態別の円

| 状態                     | 背景色                 | 説明                               |
| ------------------------ | ---------------------- | ---------------------------------- |
| **未完了（デフォルト）** | rgba(23, 23, 23, 0.64) | 濃グレー・半透明                   |
| **完了状態**             | #171717（推奨）        | より濃い、またはチェックマーク表示 |

### データ例

```json
{
  "circleState": "uncompleted", // "completed" or "uncompleted"
  "circleColor": "rgba(23, 23, 23, 0.64)"
}
```

---

### 2-2. タスク内容テキスト（text）

#### テキストスタイル

| プロパティ           | 値                     |
| -------------------- | ---------------------- |
| **要素名**           | text（またはタスク名） |
| **フォント**         | Noto Sans JP           |
| **フォントサイズ**   | 14px                   |
| **フォントウェイト** | 700（Bold）            |
| **行高**             | 19.6px（1.4em）        |
| **文字間**           | 標準                   |
| **色**               | #171717（濃グレー）    |
| **配置**             | LEFT / CENTER          |
| **サイズモード**     | Hug（自動）            |

#### テキスト内容例

- "タスクだよ"（サンプル）
- 実装時には、記事データから取得したタスク文字列に置き換え

### データ例

```json
{
  "taskId": "task_001",
  "taskText": "タスクだよ",
  "completed": false
}
```

---

## 📦 完全なデータ構造（JSON）

### 最小限のデータ構造

```json
{
  "todo": {
    "sectionTitle": "身につけること",
    "items": [
      {
        "id": "todo_item_001",
        "text": "タスク内容1",
        "completed": false
      },
      {
        "id": "todo_item_002",
        "text": "タスク内容2",
        "completed": false
      },
      {
        "id": "todo_item_003",
        "text": "タスク内容3",
        "completed": false
      }
    ]
  }
}
```

### 拡張データ構造（管理用）

```json
{
  "todo": {
    "sectionId": "todo_section_001",
    "sectionTitle": "身につけること",
    "contentId": "article_001",
    "items": [
      {
        "id": "todo_item_001",
        "text": "基本的なToDoリストのUIパターンを理解する",
        "completed": false,
        "order": 1,
        "createdAt": "2025-01-15T10:00:00Z",
        "updatedAt": "2025-01-15T10:00:00Z"
      },
      {
        "id": "todo_item_002",
        "text": "コンポーネント設計の考え方を学ぶ",
        "completed": false,
        "order": 2,
        "createdAt": "2025-01-15T10:00:00Z",
        "updatedAt": "2025-01-15T10:00:00Z"
      }
    ]
  }
}
```

---

## 🎨 色彩体系

| 用途                     | 色値                   | 説明                |
| ------------------------ | ---------------------- | ------------------- |
| **背景（コンテナ）**     | #FFFFFF                | 純白                |
| **背景（ヘッダー）**     | #F5F5F5                | 薄いグレー          |
| **テキスト（ヘッダー）** | #656668                | ミディアムグレー    |
| **テキスト（項目）**     | #171717                | 濃いグレー・ほぼ黒  |
| **円（デフォルト）**     | rgba(23, 23, 23, 0.64) | 濃グレー・64%透明度 |
| **シャドウ**             | rgba(0, 0, 0, 0.08)    | 淡い黒シャドウ      |

---

## 🔤 フォント体系

| フォント         | 用途              | サイズ | ウェイト | 行高   |
| ---------------- | ----------------- | ------ | -------- | ------ |
| **Noto Sans JP** | ヘッダータイトル  | 12px   | 700      | 24px   |
| **Noto Sans JP** | TODO 項目テキスト | 14px   | 700      | 19.6px |

---

## 📏 スペーシング定義

| 箇所                     | サイズ   | 説明                           |
| ------------------------ | -------- | ------------------------------ |
| **コンテナ外パディング** | 2px      | 外側枠線用                     |
| **ヘッダーパディング**   | 5px 16px | 上下 5px、左右 16px            |
| **コンテンツパディング** | 12px 8px | 上下 12px、左右 8px            |
| **ヘッダー内ギャップ**   | 10px     | アイコン（未使用）とテキスト間 |
| **項目間ギャップ**       | 12px     | 各 TODO 項目間                 |
| **項目内ギャップ**       | 10px     | 円とテキスト間                 |
| **項目内パディング**     | 0px 8px  | 左右 8px                       |

---

## 🎭 ビジュアル定義

### ボーダーラジアス

| 箇所             | サイズ            | 説明         |
| ---------------- | ----------------- | ------------ |
| **コンテナ全体** | 12px              | 全角         |
| **ヘッダー**     | 10px 10px 0px 0px | 上部のみ丸角 |
| **チェック円**   | 10000px           | 完全な円     |

### シャドウ

| 箇所         | 値                               | 説明             |
| ------------ | -------------------------------- | ---------------- |
| **コンテナ** | 1px 1px 4px 0px rgba(0,0,0,0.08) | 淡い影、カード感 |

---

## 🔄 インタラクション設定

### TODO 項目のクリック動作

**動作**

```
item_todo をクリック
  ↓
completed フラグをトグル（false ↔ true）
  ↓
UI状態を更新
```

### 状態遷移

#### 未完了 → 完了状態への変化

```
Before:
- circle: rgba(23, 23, 23, 0.64)（グレー）
- text: #171717（濃グレー）

After:
- circle: #171717 または チェックマークアイコン表示
- text: #171717（またはグレー #4A5565 に変更）
- 可能であればテキストに取り消し線を付与
```

### 実装時の状態管理コード例（疑似コード）

```javascript
handleTodoItemClick(itemId) {
  const item = todoItems.find(i => i.id === itemId);
  item.completed = !item.completed;

  // UI更新
  updateItemUI(itemId, {
    circleState: item.completed ? 'completed' : 'uncompleted',
    textDecoration: item.completed ? 'line-through' : 'none',
    textColor: item.completed ? '#A0A0A0' : '#171717'
  });

  // サーバー同期（推奨）
  syncToServer(itemId, item.completed);
}
```

---

## 📱 レスポンシブ対応

### デスクトップ（720px+）

- コンテナ幅: 720px（固定）
- ヘッダーパディング: 5px 16px
- コンテンツパディング: 12px 8px
- すべての TODO 項目が 1 行で表示

### タブレット（600px - 720px）

- コンテナ幅: 100%（または max-width: 720px）
- パディング比率は維持
- テキストの折返しに対応

### モバイル（360px - 600px）

- コンテナ幅: 100%（-2px マージン等で調整）
- ヘッダーパディング: 4px 12px（縮小推奨）
- コンテンツパディング: 10px 6px
- テキストがコンテナ幅を超えないよう制御
- 円とテキストは常に 1 行で表示

---

## ✅ 実装チェックリスト

- [ ] コンテナ幅 720px、背景色#FFFFFF、ボーダーラジアス 12px
- [ ] 外側パディング 2px、シャドウ効果 1px 1px 4px 0px rgba(0,0,0,0.08)
- [ ] ヘッダーブロック背景#F5F5F5、上部のみ丸角（10px 10px 0px 0px）
- [ ] ヘッダータイトル「身につけること」表示（Noto Sans JP, 12px, 700）
- [ ] コンテンツエリアパディング 12px 8px
- [ ] TODO 項目間のギャップ 12px
- [ ] 各項目に 6x6px の円（rgba(23, 23, 23, 0.64)）を配置
- [ ] 各項目にテキスト（Noto Sans JP, 14px, 700, #171717）を配置
- [ ] 項目間ギャップ 10px（円とテキスト間）
- [ ] JSON 形式のデータ構造を確認
- [ ] 複数項目のリスト表示に対応
- [ ] TODO 項目クリック時の状態変更（completed フラグトグル）
- [ ] 完了状態の視覚的フィードバック（色変更、取り消し線など）
- [ ] モバイル対応テスト（リレスポンシブ確認）
- [ ] アクセシビリティ対応（aria-label 等）

---

## 🔌 API 連携想定

### Rich テキストエリアからのデータ取得

記事コンテンツデータに以下の構造が含まれることを想定：

```json
{
  "article": {
    "id": "article_001",
    "title": "...",
    "content": "...",
    "learningObjectives": {
      // ← TODOセクション用
      "title": "身につけること",
      "items": [
        { "id": "todo_001", "text": "...", "completed": false },
        { "id": "todo_002", "text": "...", "completed": false }
      ]
    }
  }
}
```

### バックエンド対応想定

```
GET /articles/{articleId}/todos
  → 記事のTODO一覧を取得

PUT /articles/{articleId}/todos/{todoId}
  → TODO項目のcompleted状態を更新

POST /articles/{articleId}/todos
  → 新しいTODO項目を追加（管理画面用）

DELETE /articles/{articleId}/todos/{todoId}
  → TODO項目を削除（管理画面用）
```

---

## 📊 サンプル実装データ

### 実装例 1: 基本 3 項目

```json
{
  "todo": {
    "sectionTitle": "身につけること",
    "items": [
      {
        "id": "todo_001",
        "text": "基本的なToDoリストのUIパターンを理解する",
        "completed": false
      },
      {
        "id": "todo_002",
        "text": "ユーザーのニーズと実装の考え方を把握する",
        "completed": false
      },
      {
        "id": "todo_003",
        "text": "プロトタイプを通じた検証方法を学ぶ",
        "completed": false
      }
    ]
  }
}
```

### 実装例 2: 完了済みアイテムを含む

```json
{
  "todo": {
    "sectionTitle": "身につけること",
    "items": [
      {
        "id": "todo_001",
        "text": "基本的なToDoリストのUIパターンを理解する",
        "completed": true
      },
      {
        "id": "todo_002",
        "text": "ユーザーのニーズと実装の考え方を把握する",
        "completed": true
      },
      {
        "id": "todo_003",
        "text": "プロトタイプを通じた検証方法を学ぶ",
        "completed": false
      }
    ]
  }
}
```

---

## 🚀 マークアップ例（HTML/React）

### React コンポーネント例

```jsx
import React, { useState } from "react";

export function TodoSection({ data }) {
  const [todos, setTodos] = useState(data.items);

  const handleToggleTodo = (id) => {
    setTodos(
      todos.map((todo) =>
        todo.id === id ? { ...todo, completed: !todo.completed } : todo
      )
    );
  };

  return (
    <div
      className="todo-section"
      style={{
        width: "720px",
        backgroundColor: "#FFFFFF",
        borderRadius: "12px",
        boxShadow: "1px 1px 4px 0px rgba(0, 0, 0, 0.08)",
        padding: "2px",
      }}
    >
      <div
        className="heading-block"
        style={{
          backgroundColor: "#F5F5F5",
          borderRadius: "10px 10px 0px 0px",
          padding: "5px 16px",
          gap: "10px",
        }}
      >
        <h3
          style={{
            fontFamily: "Noto Sans JP",
            fontSize: "12px",
            fontWeight: "700",
            color: "#656668",
            margin: 0,
          }}
        >
          {data.sectionTitle}
        </h3>
      </div>

      <div
        className="wrap-content"
        style={{
          padding: "12px 8px",
          gap: "12px",
        }}
      >
        {todos.map((todo) => (
          <div
            key={todo.id}
            className="item-todo"
            onClick={() => handleToggleTodo(todo.id)}
            style={{
              display: "flex",
              alignItems: "center",
              gap: "10px",
              padding: "0px 8px",
              cursor: "pointer",
            }}
          >
            <div
              className="circle"
              style={{
                width: "6px",
                height: "6px",
                borderRadius: "10000px",
                backgroundColor: todo.completed
                  ? "#171717"
                  : "rgba(23, 23, 23, 0.64)",
              }}
            />
            <span
              style={{
                fontFamily: "Noto Sans JP",
                fontSize: "14px",
                fontWeight: "700",
                color: todo.completed ? "#A0A0A0" : "#171717",
                textDecoration: todo.completed ? "line-through" : "none",
              }}
            >
              {todo.text}
            </span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

### HTML マークアップ例

```html
<div class="todo-section">
  <div class="heading-block">
    <h3>身につけること</h3>
  </div>
  <div class="wrap-content">
    <div class="item-todo">
      <div class="circle"></div>
      <span class="todo-text">タスク内容1</span>
    </div>
    <div class="item-todo">
      <div class="circle"></div>
      <span class="todo-text">タスク内容2</span>
    </div>
  </div>
</div>
```

### CSS 例

```css
.todo-section {
  width: 720px;
  background-color: #ffffff;
  border-radius: 12px;
  box-shadow: 1px 1px 4px 0px rgba(0, 0, 0, 0.08);
  padding: 2px;
}

.heading-block {
  background-color: #f5f5f5;
  border-radius: 10px 10px 0px 0px;
  padding: 5px 16px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.heading-block h3 {
  font-family: "Noto Sans JP";
  font-size: 12px;
  font-weight: 700;
  color: #656668;
  margin: 0;
  line-height: 24px;
}

.wrap-content {
  padding: 12px 8px;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.item-todo {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 0px 8px;
  cursor: pointer;
}

.item-todo .circle {
  width: 6px;
  height: 6px;
  border-radius: 10000px;
  background-color: rgba(23, 23, 23, 0.64);
  flex-shrink: 0;
}

.item-todo.completed .circle {
  background-color: #171717;
}

.todo-text {
  font-family: "Noto Sans JP";
  font-size: 14px;
  font-weight: 700;
  color: #171717;
  line-height: 19.6px;
}

.item-todo.completed .todo-text {
  color: #a0a0a0;
  text-decoration: line-through;
}
```

---

## 📋 トラブルシューティング

### Q: テキストが長い場合の対応は？

**A:** テキストが幅を超える場合、自動折返しを実装してください。`word-break: break-word` または `word-wrap: break-word` を設定。

### Q: TODO 項目を動的に追加・削除できる？

**A:** 管理画面では可能。ユーザー表示画面では通常は固定です。API 対応は本仕様書の「API 連携想定」を参照。

### Q: 完了状態の保存方法は？

**A:** ユーザーごと、記事ごと、TODO 項目ごとに `completed` フラグをサーバーに保存します。

### Q: モバイルでのレイアウト崩れについて

**A:** `max-width: 100%` とし、パディングを適宜調整。画像を参照し、デザイン通りに実装してください。

---

## 🔍 検証チェック項目

実装後に以下を確認してください：

- [ ] 視覚的に仕様と一致しているか（Figma デザイン対比）
- [ ] ピクセル完全に実装されているか（寸法・色・フォント）
- [ ] クリック時の状態変更が正常に動作するか
- [ ] 複数項目のスクロール表示に対応しているか
- [ ] モバイルデバイスで正常に表示されるか
- [ ] アクセシビリティ対応（スクリーンリーダー対応）
- [ ] パフォーマンステスト（大量項目時の表示速度）
