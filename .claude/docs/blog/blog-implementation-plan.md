# Blog機能 実装計画

作成日: 2025-11-08
ブランチ: feature/blog
基準: blog-edit-requests.md

## 実装の概要

ユーザーからの要望を精査し、以下の優先順位で実装します。

---

## Phase 1: 基本レイアウトとデザインシステムの構築 🔴

### タスク1-1: 背景色の統一
**優先度**: 🔴 高
**所要時間**: 30分

**内容:**
- `/blog` 配下の全ページで背景色を統一
- Figmaデータから抽出した色:
  - メインBG: `#ffffff` (白)
  - グラデーション: `rgba(255, 242, 227, 0.4)` → `rgba(131, 136, 250, 0.11)` (linear-gradient)

**対象ファイル:**
- `src/pages/blog/index.tsx`
- `src/pages/blog/detail.tsx`
- `src/pages/blog/category.tsx`

**実装内容:**
```tsx
// 背景のグラデーションを追加
<div className="min-h-screen bg-gradient-to-br from-[#fff2e3]/40 to-[#8388fa]/11">
```

**ステータス:** [ ] 未対応

---

### タスク1-2: 固定ヘッダーナビゲーションの実装
**優先度**: 🔴 高
**所要時間**: 2時間

**内容:**
- `/blog` 配下で共通の固定ヘッダーを実装
- スクロール時も常に表示（position: sticky または fixed）
- グローバルロゴを使用

**Figmaデータから抽出した仕様:**
- 高さ: 74px
- padding: 24px (上下左右)
- レイアウト: HORIZONTAL
- ロゴ幅: 88px
- ロゴ高さ: 26px

**対象ファイル:**
- `src/components/blog/BlogHeader.tsx` (新規作成)
- `src/pages/blog/index.tsx`
- `src/pages/blog/detail.tsx`
- `src/pages/blog/category.tsx`

**実装内容:**
1. `BlogHeader.tsx`コンポーネントを作成
2. 既存のグローバルロゴコンポーネントを再利用
3. 各ページで `<BlogHeader />` を配置
4. `position: sticky; top: 0;` でスクロール固定

**ステータス:** [ ] 未対応

---

## Phase 2: ヒーローセクションとコンテンツエリアの改善 🔴

### タスク2-1: 不要なブロックの削除
**優先度**: 🔴 高
**所要時間**: 10分

**内容:**
- RSSフィード生成ボタンとサイトマップ生成ボタンのブロックを削除

**対象ファイル:**
- `src/pages/blog/index.tsx:189:10`

**実装内容:**
以下のコード部分を削除:
```tsx
<div className="flex justify-center gap-4">
  <button onClick={generateAndDownloadRSS}>...</button>
  <button onClick={generateAndDownloadSitemap}>...</button>
</div>
```

**ステータス:** [ ] 未対応

---

### タスク2-2: ヒーローセクションの置き換え
**優先度**: 🔴 高
**所要時間**: 3時間

**内容:**
- 現在のページタイトルセクションを削除
- Figmaデザインに基づいたヒーローセクションに置き換え

**Figmaデータから抽出した仕様:**
- 高さ: 1388px (大きなヒーロー)
- 背景: SVG画像 (`top-image.svg`)
- コンテンツ:
  - タイトル: ベクター画像 (343x89px)
  - サブタイトル: "BONO をつくる 30 代在宅独身男性のクラフト日誌"
  - フォント: Noto Sans JP, Medium, 14px
  - 色: `#9ca3af` (gray-400相当)
  - 配置: 中央揃え

**対象ファイル:**
- `src/pages/blog/index.tsx:132:8`
- `src/components/blog/HeroSection.tsx` (新規作成)

**実装手順:**
1. `HeroSection.tsx`コンポーネントを作成
2. 背景SVGを配置（グラデーション含む）
3. タイトルとサブタイトルを配置
4. レスポンシブ対応

**ステータス:** [ ] 未対応

---

### タスク2-3: 「All Episodes」セクションの実装
**優先度**: 🟡 中
**所要時間**: 1時間

**内容:**
- ヒーローセクションの下に「All Episodes」見出しを配置

**Figmaデータから抽出した仕様:**
- テキスト: "All Episodes"
- フォント: Hind, Bold, 24px
- line-height: 48px
- letter-spacing: 0.6px
- 色: `#000000`

**対象ファイル:**
- `src/pages/blog/index.tsx`

**実装内容:**
```tsx
<div className="container max-w-[1120px] mx-auto">
  <h2 className="font-hind font-bold text-2xl leading-[48px] tracking-[0.6px]">
    All Episodes
  </h2>
</div>
```

**ステータス:** [ ] 未対応

---

## Phase 3: ブログカードコンポーネントの刷新 🔴

### タスク3-1: 新しいブログカードデザインの実装
**優先度**: 🔴 高
**所要時間**: 3時間

**内容:**
- 現在のブログカードを削除
- Figmaデザインに基づいた新しいカードに置き換え

**Figmaデータから抽出した仕様:**
- カード幅: 1120px (コンテナ幅)
- カード高さ: 152px
- 角丸: 16px
- 背景: `#ffffff` (白)
- シャドウ: `0px 1px 2px rgba(0,0,0,0.05)`
- パディング: 12px (左), 24px (右), 12px (上下)
- レイアウト: HORIZONTAL
  - 左: サムネイル背景 (350.66px x 128px, 角丸12px)
  - 右: コンテンツエリア (701.34px)
  - 間隔: 32px

**サムネイル背景色（2種類）:**
- カラー1: `#f8e5ee` (ピンク系)
- カラー2: `#e0dfea` (紫系)

**対象ファイル:**
- `src/components/blog/BlogCard.tsx:53:6`

**実装手順:**
1. 既存のカードレイアウトを削除
2. 新しいカード構造を実装
   - サムネイル背景（カラフルな背景色）
   - タイトル、投稿者、日付
3. ホバーエフェクト（オプション）
4. レスポンシブ対応

**ステータス:** [ ] 未対応

---

## Phase 4: 記事詳細ページの改善 🟡

### タスク4-1: 「ブログ一覧へ戻る」ボタンの配置変更
**優先度**: 🔴 高
**所要時間**: 30分

**内容:**
- 現在のグリッドレイアウトを削除
- パンくずの上に単独で配置

**対象ファイル:**
- `src/pages/blog/detail.tsx:313:12`

**現在の実装（推測）:**
```tsx
// グリッドレイアウト
<div className="grid ...">
  <Link to="/blog">ブログ一覧へ戻る</Link>
  ...
</div>
```

**変更後:**
```tsx
// パンくずの上に単独配置
<div className="mb-4">
  <Link
    to="/blog"
    className="inline-flex items-center text-gray-600 hover:text-gray-900"
  >
    ← ブログ一覧へ戻る
  </Link>
</div>
<nav>パンくず...</nav>
```

**ステータス:** [ ] 未対応

---

## Phase 5: レイアウト調整とレスポンシブ対応 🟡

### タスク5-1: コンテナ幅の統一
**優先度**: 🟡 中
**所要時間**: 1時間

**内容:**
- 全ページでコンテナ幅を1120pxに統一（Figmaデザインに合わせる）

**対象ファイル:**
- `src/pages/blog/index.tsx`
- `src/pages/blog/detail.tsx`
- `src/pages/blog/category.tsx`

**実装内容:**
```tsx
<div className="container max-w-[1120px] mx-auto px-4">
  ...
</div>
```

**ステータス:** [ ] 未対応

---

### タスク5-2: レスポンシブ対応
**優先度**: 🟡 中
**所要時間**: 2時間

**内容:**
- モバイル・タブレットでのレイアウト調整
- ブログカードのスタック表示
- ヒーローセクションの高さ調整

**対象ファイル:**
- 全コンポーネント

**ステータス:** [ ] 未対応

---

## Phase 6: パフォーマンスと品質向上 🟢

### タスク6-1: 画像の最適化
**優先度**: 🟢 低
**所要時間**: 1時間

**内容:**
- SVG画像の最適化
- 遅延読み込みの実装

**ステータス:** [ ] 未対応

---

### タスク6-2: アニメーション追加
**優先度**: 🟢 低
**所要時間**: 1時間

**内容:**
- ページ遷移アニメーション
- カードホバーアニメーション

**ステータス:** [ ] 未対応

---

## 実装の進め方

### ステップ1: Phase 1を完了（1日目）
1. タスク1-1: 背景色の統一
2. タスク1-2: 固定ヘッダーナビゲーションの実装

### ステップ2: Phase 2を完了（2日目）
1. タスク2-1: 不要なブロックの削除
2. タスク2-2: ヒーローセクションの置き換え
3. タスク2-3: 「All Episodes」セクションの実装

### ステップ3: Phase 3を完了（3日目）
1. タスク3-1: 新しいブログカードデザインの実装

### ステップ4: Phase 4 & 5を完了（4日目）
1. タスク4-1: 「ブログ一覧へ戻る」ボタンの配置変更
2. タスク5-1: コンテナ幅の統一
3. タスク5-2: レスポンシブ対応

### ステップ5: Phase 6（オプション）
1. パフォーマンス最適化
2. アニメーション追加

---

## 質問・確認事項

### 質問1: 背景画像
要望2で「image.png」が参照されていますが、具体的な画像ファイルの場所がわかりません。
- Figmaから背景SVGをエクスポートする必要がありますか？
- それとも、既存のアセットを使用しますか？

### 質問2: カラフルなサムネイル背景
ブログカードのサムネイル背景色（ピンク `#f8e5ee`、紫 `#e0dfea`）は:
- ランダムに選択？
- カテゴリに応じて変更？
- 記事ごとに設定？

### 質問3: フォント
Figmaデザインで「Hind」フォントが使われていますが:
- プロジェクトに追加する必要がありますか？
- それとも代替フォントを使いますか？

---

## 次のアクション

1. 上記の質問に回答いただく
2. Phase 1 のタスク1-1から順番に実装開始
3. 各タスク完了後、動作確認
4. 問題があればblog-edit-requests.mdに記載

準備ができたら、「Phase 1から始めてください」と指示してください！
